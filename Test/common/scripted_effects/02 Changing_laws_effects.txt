increase_economic_mobilisation = {
	#change the law if available, else just give the PP amount
	if = {
		limit = {
			has_idea = civilian_economy
			#low_economic_mobilisation_available = yes
		}
		swap_ideas = {
			remove_idea = civilian_economy
			add_idea = low_economic_mobilisation
		}
	}
	else_if = {
		limit = {
			has_idea = low_economic_mobilisation
			#partial_economic_mobilisation_available = yes
		}
		swap_ideas = {
			remove_idea = low_economic_mobilisation
			add_idea = partial_economic_mobilisation
		}
	}
	else_if = {
		limit = {
			has_idea = partial_economic_mobilisation
			#war_economy_available = yes
		}
		swap_ideas = {
			remove_idea = partial_economic_mobilisation
			add_idea = war_economy
		}
	}
	else_if = {
		limit = {
			has_idea = war_economy
			#tot_economic_mobilisation_available = yes
		}
		swap_ideas = {
			remove_idea = war_economy
			add_idea = tot_economic_mobilisation
		}
	}
	else = {
		add_political_power = 150
	}
}

decrease_economic_mobilisation = {
	#check which new law would be available
	if = {
		limit = {
			has_idea = tot_economic_mobilisation
		}
		set_temp_variable = {
			new_economy_law = token:war_economy
		}
	}
	else_if = {
		limit = {
			OR = {
				has_idea = tot_economic_mobilisation
				has_idea = war_economy
			}
		}
		set_temp_variable = {
			new_economy_law = token:partial_economic_mobilisation
		}
	}
	else_if = {
		limit = {
			OR = {
				has_idea = tot_economic_mobilisation
				has_idea = war_economy
				has_idea = partial_economic_mobilisation
			}
		}
		set_temp_variable = {
			new_economy_law = token:low_economic_mobilisation
		}
	}
	else = {
		set_temp_variable = {
			new_economy_law = token:civilian_economy
		}
	}
	#then swap the current law for the new one
	if = {
		limit = {
			has_idea = tot_economic_mobilisation
		}
		swap_ideas = {
			remove_idea = tot_economic_mobilisation
			add_idea = var:new_economy_law
		}
	}
	else_if = {
		limit = {
			has_idea = war_economy
		}
		swap_ideas = {
			remove_idea = war_economy
			add_idea = var:new_economy_law
		}
	}
	else_if = {
		limit = {
			has_idea = partial_economic_mobilisation
		}
		swap_ideas = {
			remove_idea = partial_economic_mobilisation
			add_idea = var:new_economy_law
		}
	}
	else_if = {
		limit = {
			has_idea = low_economic_mobilisation
		}
		swap_ideas = {
			remove_idea = low_economic_mobilisation
			add_idea = var:new_economy_law
		}
	}
}

increase_conscription = {
	#change the law if available, else just give the PP amount
	if = {
		limit = {
			has_idea = disarmed_nation
		}
		swap_ideas = {
			remove_idea = disarmed_nation
			add_idea = volunteer_only
		}
	}
	else_if = {
		limit = {
			has_idea = volunteer_only
		}
		swap_ideas = {
			remove_idea = volunteer_only
			add_idea = limited_conscription
		}
	}
	else_if = {
		limit = {
			has_idea = limited_conscription
		}
		swap_ideas = {
			remove_idea = limited_conscription
			add_idea = extensive_conscription
		}
	}
	else_if = {
		limit = {
			has_idea = extensive_conscription
		}
		swap_ideas = {
			remove_idea = extensive_conscription
			add_idea = service_by_requirement
		}
	}
	else_if = {
		limit = {
			has_idea = service_by_requirement
		}
		swap_ideas = {
			remove_idea = service_by_requirement
			add_idea = all_adults_serve
		}
	}
	else_if = {
		limit = {
			has_idea = all_adults_serve
		}
		swap_ideas = {
			remove_idea = all_adults_serve
			add_idea = scraping_the_barrel
		}
	}
	else = {
		add_political_power = 150
	}
}

decrease_conscription = {
	#check which new law would be available
	if = {
		limit = {
			has_idea = scraping_the_barrel
		}
		set_temp_variable = {
			new_conscription_law = token:all_adults_serve
		}
	}
	else_if = {
		limit = {
			OR = {
				has_idea = scraping_the_barrel
				has_idea = all_adults_serve
			}
		}
		set_temp_variable = {
			new_conscription_law = token:service_by_requirement
		}
	}
	else_if = {
		limit = {
			OR = {
				has_idea = scraping_the_barrel
				has_idea = all_adults_serve
				has_idea = service_by_requirement
			}
		}
		set_temp_variable = {
			new_conscription_law = token:extensive_conscription
		}
	}
	else_if = {
		limit = {
			OR = {
				has_idea = scraping_the_barrel
				has_idea = all_adults_serve
				has_idea = service_by_requirement
				has_idea = extensive_conscription
			}
		}
		set_temp_variable = {
			new_conscription_law = token:limited_conscription
		}
	}
	else = {
		set_temp_variable = {
			new_conscription_law = token:volunteer_only
		}
	}
	#then swap the current law for the new one
	if = {
		limit = {
			has_idea = scraping_the_barrel
		}
		swap_ideas = {
			remove_idea = scraping_the_barrel
			add_idea = var:new_conscription_law
		}
	}
	else_if = {
		limit = {
			has_idea = all_adults_serve
		}
		swap_ideas = {
			remove_idea = all_adults_serve
			add_idea = var:new_conscription_law
		}
	}
	else_if = {
		limit = {
			has_idea = service_by_requirement
		}
		swap_ideas = {
			remove_idea = service_by_requirement
			add_idea = var:new_conscription_law
		}
	}
	else_if = {
		limit = {
			has_idea = extensive_conscription
		}
		swap_ideas = {
			remove_idea = extensive_conscription
			add_idea = var:new_conscription_law
		}
	}
	else_if = {
		limit = {
			has_idea = limited_conscription
		}
		swap_ideas = {
			remove_idea = limited_conscription
			add_idea = var:new_conscription_law
		}
	}
}

increase_trade_law = {
	#change the law if available, else just give the PP amount
	if = {
		limit = {
			has_idea = closed_economy
		}
		swap_ideas = {
			remove_idea = closed_economy
			add_idea = limited_exports
		}
	}
	else_if = {
		limit = {
			has_idea = limited_exports
		}
		swap_ideas = {
			remove_idea = limited_exports
			add_idea = export_focus
		}
	}
	else_if = {
		limit = {
			has_idea = export_focus
		}
		swap_ideas = {
			remove_idea = export_focus
			add_idea = free_trade
		}
	}
	else = {
		add_political_power = 150
	}
}

decrease_trade_law = {
	#check which new law would be available
	if = {
		limit = {
			has_idea = free_trade
		}
		set_temp_variable = {
			new_trade_law = token:export_focus
		}
	}
	else_if = {
		limit = {
			OR = {
				has_idea = free_trade
				has_idea = export_focus
			}
		}
		set_temp_variable = {
			new_trade_law = token:limited_exports
		}
	}
	else_if = {
		limit = {
			OR = {
				has_idea = free_trade
				has_idea = export_focus
				has_idea = limited_exports
			}
		}
		set_temp_variable = {
			new_trade_law = token:closed_economy
		}
	}
	#then swap the current law for the new one
	if = {
		limit = {
			has_idea = free_trade
		}
		swap_ideas = {
			remove_idea = free_trade
			add_idea = var:new_trade_law
		}
	}
	else_if = {
		limit = {
			has_idea = export_focus
		}
		swap_ideas = {
			remove_idea = export_focus
			add_idea = var:new_trade_law
		}
	}
	else_if = {
		limit = {
			has_idea = limited_exports
		}
		swap_ideas = {
			remove_idea = limited_exports
			add_idea = var:new_trade_law
		}
	}
	else_if = {
		limit = {
			has_idea = limited_exports
		}
		swap_ideas = {
			remove_idea = limited_exports
			add_idea = var:new_trade_law
		}
	}
}

swap_to_free_trade = {
	if = {
		limit = {
			has_idea = export_focus
		}
		swap_ideas = {
			remove_idea = export_focus
			add_idea = free_trade
		}
	}
	if = {
		limit = {
			has_idea = limited_exports
		}
		swap_ideas = {
			remove_idea = limited_exports
			add_idea = free_trade
		}
	}
	if = {
		limit = {
			has_idea = closed_economy
		}
		swap_ideas = {
			remove_idea = closed_economy
			add_idea = free_trade
		}
	}
}
