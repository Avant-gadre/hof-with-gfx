#Category only allowed for GER
GER_reichskommissariats = {

	RK_Local_Army_Training_Program_dec = {
	
		icon = generic_construction
		
	fire_only_once = yes
	
	days_remove = 90
	
	visible = {
	original_tag = GER
	has_capitulated = no
	is_subject = no
	is_faction_leader = yes
	FROM = {
		is_in_faction_with = GER
	}
	FROM = {
		is_subject_of = GER
	}
	FROM = { exists = yes }
	}
	
	target_trigger = {
	FROM = {
		OR = {
		original_tag = RKP
		original_tag = RKU
		original_tag = RKR
		original_tag = RKH
		original_tag = RKC
		original_tag = RKF
		original_tag = RKI
		original_tag = RKE
		original_tag = RKO
		original_tag = RKM
		original_tag = RKA
		original_tag = RUL
		original_tag = RTK
		original_tag = RIB
		original_tag = RKN
		}
	}
	}

	available = {
	num_of_civilian_factories_available_for_projects > 4
	FROM = {
		is_in_faction_with = GER
	}
	FROM = {
		is_subject_of = GER
	}
	FROM = {
		has_capitulated = no
	}
	FROM = { exists = yes }
	}

	cost = 50

	cancel_trigger = {
	or = {
		FROM = {
		NOT = { is_in_faction_with = GER }
		}
		FROM = {
		is_subject = no
		}
		FROM = {
		has_capitulated = yes
		}
		FROM = { exists = no }
	}
	}

	modifier = {
	civilian_factory_use = 5
	}

	remove_effect = {
	FROM = {

			army_experience = 50

			air_experience = 25

		add_equipment_to_stockpile = {
		type = infantry_equipment
		amount = 10000
		producer = GER
		}

		add_equipment_to_stockpile = {
		type = support_equipment
		amount = 500
		producer = GER
		}

		add_equipment_to_stockpile = {
		type = artillery_equipment
		amount = 900
		producer = GER
		}

		add_equipment_to_stockpile = {
		type = anti_air_equipment
		amount = 300
		producer = GER
		}

		add_equipment_to_stockpile = {
		type = anti_tank_equipment
		amount = 300
		producer = GER
		}

		add_equipment_to_stockpile = {
		type = fighter_equipment
		amount = 150
		producer = GER
		}

		add_equipment_to_stockpile = {
		type = tac_bomber_equipment
		amount = 75
		producer = GER
		}

		add_timed_idea = {
		idea = RK_Local_Army_Training_Program_idea
		days = 90
		}

		
	}
	}
	
	ai_will_do = {
	factor = 30
	modifier = {
		any_home_area_neighbor_country = {
		has_war_with = GER
		}
		factor = 0
	}
	}
}

	GER_qingdao = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	OR = {
		GER = { has_full_control_of_state = 743 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}

		is_in_faction_with = GER
		has_full_control_of_state = 743
		}
	}

		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
	is_subject = no
	has_country_flag = GER_qingdao_renamed_flag
		}

		complete_effect = {
			QDA = {
		add_state_core = 743
				transfer_state = 743

				hidden_effect = {
					load_oob = "QDA_1936"
				}

				GER = {
					puppet = QDA


					if = {
						limit = {
							NOT = { has_dlc = "Death or Dishonor" }
						}
						set_autonomy = { target = QDA autonomy_state = autonomy_integrated_puppet }
					}
					else = {
						set_autonomy = { target = QDA autonomy_state = autonomy_reichskommissariat }
					}
				}

				set_cosmetic_tag = QDA_GER

		set_politics = {
			ruling_party = neutrality
			elections_allowed = no
		}  

		add_popularity = {
		ideology = neutrality
		popularity = 0.65
		}  

		if = {
		limit = {
			GER = { has_idea = ger_cooperate_with_japan }
		}

		QDA = { give_military_access = JAP }
		}

			}
		}
	}

	GER_great_HOL = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	has_country_flag = Formation_rk_RKF

	OR = {
		GER = { has_full_control_of_state = 6 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 6
		}
	}
	OR = {
		GER = { has_full_control_of_state = 34 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 34
		}
	}
	OR = {
		GER = { has_full_control_of_state = 8 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 8
		}
	}
	OR = {
		GER = { has_full_control_of_state = 29 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}	
		is_in_faction_with = GER
		has_full_control_of_state = 29
		}
	}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
	is_subject = no
	HOL = { is_subject = no }
	is_in_faction_with = HOL
		}

		complete_effect = {
			HOL = {
		add_state_core = 6
		add_state_core = 34
		add_state_core = 8
		add_state_core = 29
				transfer_state = 6
				transfer_state = 34
				transfer_state = 8
				transfer_state = 29
			}
		}
	}

	GER_great_SWE = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	has_country_flag = ostland
	OR = {
		GER = { has_full_control_of_state = 11 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 11
		}
	}
	OR = {
		GER = { has_full_control_of_state = 189 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 189
		}
	}
	OR = {
		GER = { has_full_control_of_state = 190 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 190
		}
	}
	OR = {
		GER = { has_full_control_of_state = 12 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 12
		}
	}
	OR = {
		GER = { has_full_control_of_state = 191 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 191
		}
	}
	OR = {
		GER = { has_full_control_of_state = 13 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 13
		}
	}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
	always = no
			has_government = fascism
	is_subject = no
	SWE = { is_subject = no }
	is_in_faction_with = SWE
		}

		complete_effect = {
			SWE = {
				add_state_claim = 11
				add_state_claim = 189
				add_state_claim = 190
				add_state_claim = 12
				add_state_claim = 191
				add_state_claim = 13

				transfer_state = 11
				transfer_state = 189
				transfer_state = 190
				transfer_state = 12
				transfer_state = 191
				transfer_state = 13
			}
		}
	}

	GER_great_SWE_north = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	OR = {
		GER = { has_full_control_of_state = 148 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 148
		}
	}
	OR = {
		GER = { has_full_control_of_state = 722 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 722
		}
	}
	OR = {
		GER = { has_full_control_of_state = 147 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 147
		}
	}
	OR = {
		GER = { has_full_control_of_state = 149 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 149
		}
	}
	OR = {
		GER = { has_full_control_of_state = 111 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 111
		}
	}
	OR = {
		GER = { has_full_control_of_state = 150 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 150
		}
	}
	OR = {
		GER = { has_full_control_of_state = 146 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}	
		is_in_faction_with = GER
		has_full_control_of_state = 146
		}
	}
	OR = {
		GER = { has_full_control_of_state = 213 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 213
		}
	}
	OR = {
		GER = { has_full_control_of_state = 215 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 215
		}
	}
	OR = {
		GER = { has_full_control_of_state = 195 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 195
		}
	}
	OR = {
		GER = { has_full_control_of_state = 208 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		has_full_control_of_state = 208
		}
	}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
	has_country_flag = Formation_rk_RKM
			has_government = fascism
	is_subject = no
	SWE = { is_subject = no }
	is_in_faction_with = SWE
	NOT = {
		is_in_faction_with = FIN
		FIN = { is_subject = no }
	}
		or = {
		NOT = { is_in_faction_with = SOV }
		AND = {
		is_in_faction_with = SOV
		SOV = { is_subject = yes }
		}
		AND = {
		is_in_faction_with = SOV
		SOV = { is_subject = no }
		NOT = { SOV = { owns_state = 219 } }
		}
		NOT = { is_in_faction_with = RUS }
		AND = {
		is_in_faction_with = RUS
		RUS = { is_subject = yes }
		}
		AND = {
		is_in_faction_with = RUS
		RUS = { is_subject = no }
		NOT = { RUS = { owns_state = 219 } }
		}
		}
		}

		complete_effect = {
			SWE = {
				transfer_state = 148
				transfer_state = 722
				transfer_state = 147
				transfer_state = 149
				transfer_state = 111
				transfer_state = 150
				transfer_state = 146
				transfer_state = 213
				transfer_state = 215
				transfer_state = 195
				transfer_state = 208
			}
		}
	}

	GER_reichskommissariat_poland = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
				OR = {
					GER = { controls_state = 98 }
					any_other_country = {
			is_ai = yes
						is_in_faction_with = GER
						controls_state = 98
					}
				}
				OR = {
					GER = { controls_state = 87 }
					any_other_country = {
			is_ai = yes
						is_in_faction_with = GER
						controls_state = 87
					}
				}
				OR = {
					GER = { controls_state = 90 }
					any_other_country = {
			is_ai = yes
						is_in_faction_with = GER
						controls_state = 90
					}
				}
				OR = {
					GER = { controls_state = 88 }
					any_other_country = {
			is_ai = yes
						is_in_faction_with = GER
						controls_state = 88
					}
				}
				OR = {
					GER = { controls_state = 10 }
					any_other_country = {
			is_ai = yes
						is_in_faction_with = GER
						controls_state = 10
					}
				}
				OR = {
					GER = { controls_state = 92 }
					any_other_country = {
			is_ai = yes
						is_in_faction_with = GER
						controls_state = 92
					}
				}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
	is_subject = no
		or = {
		NOT = { is_in_faction_with = POL }
		AND = {
		is_in_faction_with = POL
		POL = { is_subject = yes }
		NOT = { POL = { has_country_flag = GER_Accept_economic_integration } }
		}
		AND = {
		is_in_faction_with = POL
		POL = { is_subject = no }
		NOT = { POL = { owns_state = 10 } }
		}
		}
		}

		complete_effect = {

			GER_increase_propaganda = yes

	add_state_claim = 87
	add_state_claim = 98
	add_state_claim = 97

			RKP = {

		add_state_core = 90
		add_state_core = 88
		add_state_core = 10
		add_state_core = 92

				transfer_state = 90
				transfer_state = 88
				transfer_state = 10
				transfer_state = 92

				GER = {
					puppet = RKP
					if = {
						limit = {
							NOT = { has_dlc = "Death or Dishonor" }
						}
						set_autonomy = { target = RKP autonomy_state = autonomy_integrated_puppet }
					}
					else = {
						set_autonomy = { target = RKP autonomy_state = autonomy_reichskommissariat }
					}
				}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RKP
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RKP }
			}
			RKP = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

		if = {
		limit = {
			or = {
			GER = {	has_country_flag = GER_Strasser_becomes_the_leader_flag }
			GER = {	has_country_flag = GER_Heydrich_becomes_the_leader_flag }
			}
		}

		create_country_leader = {
			name = "Hans Frank"
			desc = "POLITICS_HANS_FRANK_DESC"
			picture = "Portrait_General_Gouvernment_Hans_Frank.dds"
			expire = "1974.4.4"
			ideology = nazism
			traits = {
			Unwelcome_commissioner
			}
		}
		}

			}

			if = {
				limit = {
					country_exists = POL
		POL = { is_subject_of = GER }
				}

				RKP = { annex_country = { target = POL } }
			}

		}
	}

	GER_reichskommissariat_ostland = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	OR = {
		GER = { controls_state = 784 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 784
		}
	}
	OR = {
		GER = { controls_state = 12 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 12
		}
	}
	OR = {
		GER = { controls_state = 13 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 13
		}
	}
	OR = {
		GER = { controls_state = 206 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 206
		}
	}
	OR = {
		GER = { controls_state = 263 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 263
		}
	}
	OR = {
		GER = { controls_state = 241 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 241
		}
	}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100

		}

		visible = {
			has_government = fascism
	is_subject = no
		or = {
		NOT = { is_in_faction_with = SOV }
		AND = {
		is_in_faction_with = SOV
		SOV = { is_subject = yes }
		}
		AND = {
		is_in_faction_with = SOV
		SOV = { is_subject = no }
		NOT = { SOV = { owns_state = 219 } }
		}
		NOT = { is_in_faction_with = RUS }
		AND = {
		is_in_faction_with = RUS
		RUS = { is_subject = yes }
		}
		AND = {
		is_in_faction_with = RUS
		RUS = { is_subject = no }
		NOT = { RUS = { owns_state = 219 } }
		}
		}
		}

		complete_effect = {
	set_country_flag = ostland

			GER_increase_propaganda = yes

	every_state = {
		limit = {
		or = {
			is_core_of = LIT
			is_core_of = LAT
			is_core_of = EST
			is_core_of = BLR
		}
		}
		add_core_of = RKO
	}

			RKO = {

		add_state_core = 210
		add_state_core = 209
		add_state_core = 263
		add_state_core = 208
		add_state_core = 244


				transfer_state = 206

				GER = {
					puppet = RKO
					if = {
						limit = {
							NOT = { has_dlc = "Death or Dishonor" }
						}
						set_autonomy = { target = RKO autonomy_state = autonomy_integrated_puppet }
					}
					else = {
						set_autonomy = { target = RKO autonomy_state = autonomy_reichskommissariat }
					}
				}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RKO
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RKO }
			}
			RKO = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

		if = {
		limit = {
			GER = { has_country_flag = GER_Dismissal_Rosenberg_flag }
		}
		RKO = {
			create_country_leader = {
			name = "Erwin von Scheubner Richter"
			desc = ""
			picture = "gfx/leaders/GER/Portrait_Baltic_Max_Erwin_von_Scheubner_Richter.tga"
			expire = "1983.4.24"
			ideology = valkist
			traits = {
				
			}
			}
		}
		}

			}

			every_state = {
				limit = {
		or = {
			is_core_of = LIT
			is_core_of = LAT
			is_core_of = EST
			is_core_of = BLR
			is_core_of = RKO
		}
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
						is_ai = yes
							has_full_control_of_state = PREV
							is_in_faction_with = GER
						}
					}
					
				}
				RKO = { transfer_state = PREV }
			}

	if = {
		limit = { 
		has_global_flag = GER_POL_Territorial_exchange_pact
		POL = { is_in_faction_with = GER }
		POL = { is_subject = no }
		}
		POL = { transfer_state = 189 }
		POL = { transfer_state = 11 }
		POL = { transfer_state = 784 }
		POL = { transfer_state = 96 }
		POL = { transfer_state = 95 }      
		POL = { transfer_state = 94 }
	}

			if = {
				limit = {
					country_exists = LIT
		LIT = { is_subject_of = GER }
				}

				RKO = { annex_country = { target = LIT } }
			}

			if = {
				limit = {
					country_exists = LAT
		LAT = { is_subject_of = GER }
				}

				RKO = { annex_country = { target = LAT } }
			}

			if = {
				limit = {
					country_exists = EST
		EST = { is_subject_of = GER }
				}

				RKO = { annex_country = { target = EST } }
			}

			if = {
				limit = {
					country_exists = BLR
		BLR = { is_subject_of = GER }
				}

				RKO = { annex_country = { target = BLR } }
			}

		}
	}

	GER_reichskommissariat_ukraine = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
			OR = {
				NOT = { is_in_faction_with = UKR }
				UKR = { is_subject = yes }
				NOT = { UKR = { owns_state = 202 } }
			}
			OR = {
				GER = { controls_state = 199 }
				any_other_country = {
				is_ai = yes
				OR = {
					is_subject = no
					AND = {
					is_subject = yes
					OVERLORD = { 
						or = { 
						is_ai = yes  
						tag = GER
						}
					}
					}
				}
				is_in_faction_with = GER
				controls_state = 199
				}
			}
			OR = {
				GER = { controls_state = 202 }
				any_other_country = {
				is_ai = yes
				OR = {
					is_subject = no
					AND = {
					is_subject = yes
					OVERLORD = { 
						or = { 
						is_ai = yes  
						tag = GER
						}
					}
					}
				}
				is_in_faction_with = GER
				controls_state = 202
				}
			}
			OR = {
				GER = { controls_state = 226 }
				any_other_country = {
				is_ai = yes
				OR = {
					is_subject = no
					AND = {
					is_subject = yes
					OVERLORD = { 
						or = { 
						is_ai = yes  
						tag = GER
						}
					}
					}
				}
				is_in_faction_with = GER
				controls_state = 226
				}
			}
			OR = {
				GER = { controls_state = 228 }
				any_other_country = {
				is_ai = yes
				OR = {
					is_subject = no
					AND = {
					is_subject = yes
					OVERLORD = { 
						or = { 
						is_ai = yes  
						tag = GER
						}
					}
					}
				}
				is_in_faction_with = GER
				controls_state = 228
				}
			}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
			is_subject = no
			or = {
				NOT = { is_in_faction_with = UKR }
				AND = {
					is_in_faction_with = UKR
					UKR = { is_subject = yes }
				}
				AND = {
					is_in_faction_with = UKR
					UKR = { is_subject = no }
					NOT = { UKR = { owns_state = 202 } }
				}

				NOT = { is_in_faction_with = SOV }
				AND = {
					is_in_faction_with = SOV
					SOV = { is_subject = yes }
				}
				AND = {
					is_in_faction_with = SOV
					SOV = { is_subject = no }
					NOT = { SOV = { owns_state = 219 } }
				}

				NOT = { is_in_faction_with = RUS }
				AND = {
					is_in_faction_with = RUS
					RUS = { is_subject = yes }
				}
				AND = {
					is_in_faction_with = RUS
					RUS = { is_subject = no }
					NOT = { RUS = { owns_state = 219 } }
				}
			}
		}

		complete_effect = {

			GER_increase_propaganda = yes

	if = {
		limit = { 
		ROM = { is_in_faction_with = GER }
		ROM = { is_subject = no }
		}
		ROM = { transfer_state = 198 }
		ROM = { transfer_state = 192 }
	}

	every_state = {
		limit = {
		is_core_of = UKR
		}
		add_core_of = RKU
	}

			RKU = {
				transfer_state = 202

				GER = {
					puppet = RKU
					if = {
						limit = {
							NOT = { has_dlc = "Death or Dishonor" }
						}
						set_autonomy = { target = RKU autonomy_state = autonomy_integrated_puppet }
					}
					else = {
						set_autonomy = { target = RKU autonomy_state = autonomy_reichskommissariat }
					}
				}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RKU
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RKU }
			}
			RKU = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

		if = {
		limit = {
			or = {
			GER = {	has_country_flag = GER_Strasser_becomes_the_leader_flag }
			GER = {	has_country_flag = GER_Heydrich_becomes_the_leader_flag }
			}
		}

		create_country_leader = {
			name = "Erich Koch"
			desc = "POLITICS_HINRICH_LOHSE_DESC"
			picture = "gfx/leaders/RKU/leader_UKR_Erich_Koch.dds"
			expire = "1965.1.1"
			ideology = valkist
			traits = {
			Unwelcome_commissioner
			}
		}
		}

			}

			every_state = {
				limit = {
		is_core_of = UKR
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
		NOT = { is_core_of = ROM }
					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
						is_ai = yes
							has_full_control_of_state = PREV
							is_in_faction_with = GER
						}
					}
					
				}
				RKU = { transfer_state = PREV }
			}

	if = {
		limit = { 
		has_global_flag = GER_POL_Territorial_exchange_pact
		POL = { is_in_faction_with = GER }
		POL = { is_subject = no }
		}
		POL = { transfer_state = 93 }
		POL = { transfer_state = 91 }
		POL = { transfer_state = 89 }
	}

			if = {
				limit = {
		country_exists = UKR
		UKR = { is_subject_of = GER }
				}

				RKU = { annex_country = { target = UKR } }
			}

		}
	}

	GER_reichskommissariat_moskau = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	has_country_flag = Formation_rk_RKR
	OR = {
		GER = { controls_state = 351 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 351
		}
	}
	OR = {
		GER = { controls_state = 219 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 219
		}
	}
	OR = {
		GER = { controls_state = 217 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 217
		}
	}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
	has_government = fascism
	is_subject = no
		}

		complete_effect = {

	set_country_flag = Formation_rk_RKM

			GER_increase_propaganda = yes

			RKM = {

				add_state_core = 213
				add_state_core = 215
				add_state_core = 214
				add_state_core = 216
				add_state_core = 264
				add_state_core = 351
				add_state_core = 246
				add_state_core = 247
				add_state_core = 248
				add_state_core = 252
				add_state_core = 253
				add_state_core = 254
				add_state_core = 219
				add_state_core = 223
				add_state_core = 222
				add_state_core = 224
				add_state_core = 241
				add_state_core = 243
				add_state_core = 205
				add_state_core = 242
				add_state_core = 220
				add_state_core = 240
				add_state_core = 258
				add_state_core = 260
				add_state_core = 257
				add_state_core = 265
				add_state_core = 245
				add_state_core = 218
				add_state_core = 255
				add_state_core = 239
				add_state_core = 217
				add_state_core = 238
				add_state_core = 237
				add_state_core = 236
				add_state_core = 407
				add_state_core = 401
				add_state_core = 406

			}

	every_state = {
		limit = {
		is_core_of = RKM
		is_core_of = RKR
		}
		remove_core_of = RKR
	}

			RKM = {
				transfer_state = 219

		GER = {
		puppet = RKM
		if = {
			limit = {
			NOT = { has_dlc = "Death or Dishonor" }
			}
			set_autonomy = { target = RKM autonomy_state = autonomy_integrated_puppet }
		}
		else = {
			set_autonomy = { target = RKM autonomy_state = autonomy_reichskommissariat }
		}
		}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RKM
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RKM }
			}
			RKM = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

			}

			every_state = {
				limit = {
		is_core_of = RKM
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
						is_ai = yes
							has_full_control_of_state = PREV
							is_in_faction_with = GER
						}
					}
					
				}
				RKM = { transfer_state = PREV }
			}

			if = {
				limit = { 
					UKR = { is_in_faction_with = GER }
					UKR = { is_subject = no }
				}
				UKR = { transfer_state = 218 }
			}

		}
	}

	GER_reichskommissariat_Caucasian = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	has_country_flag = Formation_rk_RKR
	OR = {
		GER = { controls_state = 234 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 234
		}
	}
	OR = {
		GER = { controls_state = 229 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 229
		}
	}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
	has_government = fascism
	is_subject = no
		}

		complete_effect = {

			GER_increase_propaganda = yes

			RKA = {

				add_state_core = 229
				add_state_core = 234
				add_state_core = 235
				add_state_core = 232
				add_state_core = 233
				add_state_core = 231
				add_state_core = 230
			}

	every_state = {
		limit = {
		is_core_of = RKA
		is_core_of = RKR
		}
		remove_core_of = RKR
	}

			RKA = {
				transfer_state = 229

		GER = {
		puppet = RKA
		if = {
			limit = {
			NOT = { has_dlc = "Death or Dishonor" }
			}
			set_autonomy = { target = RKA autonomy_state = autonomy_integrated_puppet }
		}
		else = {
			set_autonomy = { target = RKA autonomy_state = autonomy_reichskommissariat }
		}
		}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RKA
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RKA }
			}
			RKA = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

			}

			every_state = {
				limit = {
		is_core_of = RKA
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
						is_ai = yes
							has_full_control_of_state = PREV
							is_in_faction_with = GER
						}
					}
					
				}
				RKA = { transfer_state = PREV }
			}

			if = {
				limit = { 
					UKR = { is_in_faction_with = GER }
					UKR = { is_subject = no }
				}
				UKR = { transfer_state = 234 }
			}

		}
	}

	GER_reichskommissariat_Ural = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	has_country_flag = Formation_rk_RKR
	OR = {
		GER = { controls_state = 249 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 249
		}
	}
	OR = {
		GER = { controls_state = 573 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}	
		is_in_faction_with = GER
		controls_state = 573
		}
	}
	OR = {
		GER = { controls_state = 582 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}	
		is_in_faction_with = GER
		controls_state = 582
		}
	}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
	has_government = fascism
	is_subject = no
		}

		complete_effect = {

			GER_increase_propaganda = yes

			RUL = {

				add_state_core = 262
				add_state_core = 397
				add_state_core = 400
				add_state_core = 256
				add_state_core = 249
				add_state_core = 250
				add_state_core = 398
				add_state_core = 399
				add_state_core = 651
				add_state_core = 652
				add_state_core = 581
				add_state_core = 573
				add_state_core = 582
				add_state_core = 251
			}

	every_state = {
		limit = {
		is_core_of = RUL
		is_core_of = RKR
		}
		remove_core_of = RKR
	}

			RUL = {
				transfer_state = 249

		GER = {
		puppet = RUL
		if = {
			limit = {
			NOT = { has_dlc = "Death or Dishonor" }
			}
			set_autonomy = { target = RUL autonomy_state = autonomy_integrated_puppet }
		}
		else = {
			set_autonomy = { target = RUL autonomy_state = autonomy_reichskommissariat }
		}
		}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RUL
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RUL }
			}
			RUL = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

			}

			every_state = {
				limit = {
		is_core_of = RUL
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
						is_ai = yes
							has_full_control_of_state = PREV
							is_in_faction_with = GER
						}
					}
					
				}
				RUL = { transfer_state = PREV }
			}

		}
	}

	GER_reichskommissariat_Turkista = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	has_country_flag = Formation_rk_RKR
	OR = {
		GER = { controls_state = 404 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 404
		}
	}
	OR = {
		GER = { controls_state = 405 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 405
		}
	}
	OR = {
		GER = { controls_state = 584 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 584
		}
	}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
	has_government = fascism
	is_subject = no
		}

		complete_effect = {

			GER_increase_propaganda = yes

			RTK = {

				add_state_core = 402
				add_state_core = 587
				add_state_core = 584
				add_state_core = 583
				add_state_core = 404
				add_state_core = 585
				add_state_core = 405
				add_state_core = 590
				add_state_core = 588
				add_state_core = 589
				add_state_core = 586
				add_state_core = 732
				add_state_core = 742
			}

	every_state = {
		limit = {
		is_core_of = RTK
		is_core_of = RKR
		}
		remove_core_of = RKR
	}

			RTK = {
				transfer_state = 405

		GER = {
		puppet = RTK
		if = {
			limit = {
			NOT = { has_dlc = "Death or Dishonor" }
			}
			set_autonomy = { target = RTK autonomy_state = autonomy_integrated_puppet }
		}
		else = {
			set_autonomy = { target = RTK autonomy_state = autonomy_reichskommissariat }
		}
		}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RTK
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RTK }
			}
			RTK = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

			}

			every_state = {
				limit = {
		is_core_of = RTK
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
						is_ai = yes
							has_full_control_of_state = PREV
							is_in_faction_with = GER
						}
					}
					
				}
				RTK = { transfer_state = PREV }
			}

		}
	}

	GER_reichskommissariat_Russia = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	OR = {
		GER = { controls_state = 195 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 195
		}
	}

	OR = {
		GER = { controls_state = 219 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 219
		}
	}

	OR = {
		GER = { controls_state = 217 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 217
		}
	}

	OR = {
		GER = { controls_state = 229 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 229
		}
	}

	OR = {
		SOV = { has_capitulated = yes }
		NOT = { country_exists = SOV }
		SOV = { is_subject = yes }
		AND = {
		country_exists = SOV 
		SOV = { is_subject = no }
		NOT = { SOV = { controls_state = 219 } }
		}
		RUS = { has_capitulated = yes }
		NOT = { country_exists = RUS }
		RUS = { is_subject = yes }
		AND = {
		country_exists = RUS 
		RUS = { is_subject = no }
		NOT = { RUS = { controls_state = 219 } }
		}
	}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
	is_subject = no
		or = {
		NOT = { is_in_faction_with = SOV }
		AND = {
		is_in_faction_with = SOV
		SOV = { is_subject = yes }
		}
		AND = {
		is_in_faction_with = SOV
		SOV = { is_subject = no }
		NOT = { SOV = { owns_state = 219 } }
		}
		NOT = { is_in_faction_with = RUS }
		AND = {
		is_in_faction_with = RUS
		RUS = { is_subject = yes }
		}
		AND = {
		is_in_faction_with = RUS
		RUS = { is_subject = no }
		NOT = { RUS = { owns_state = 219 } }
		}
		}
		}

		complete_effect = {

			GER_increase_propaganda = yes

	add_state_claim = 195
	add_state_claim = 137

	set_country_flag = Formation_rk_RKR

	every_state = {
		limit = {
		is_core_of = SOV
		NOT = { is_core_of = LIT }
		NOT = { is_core_of = LAT }
		NOT = { is_core_of = EST }
		NOT = { is_core_of = BLR }
		NOT = { is_core_of = RKO }
		NOT = { is_core_of = UKR } 
		}
		add_core_of = RKR
	}

			RKR = {
				transfer_state = 219

				GER = {
					puppet = RKR
					if = {
						limit = {
							NOT = { has_dlc = "Death or Dishonor" }
						}
						set_autonomy = { target = RKR autonomy_state = autonomy_integrated_puppet }
					}
					else = {
						set_autonomy = { target = RKR autonomy_state = autonomy_reichskommissariat }
					}
				}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RKR
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RKR }
			}
			RKR = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

			}

			every_state = {
				limit = {
					is_core_of = SOV
		NOT = { is_core_of = RKU }
		NOT = { is_core_of = RKP }
		NOT = { is_core_of = RKO }
		NOT = { is_core_of = UKR }
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
						is_ai = yes
							has_full_control_of_state = PREV
							is_in_faction_with = GER
						}
					}
					
				}
				RKR = { transfer_state = PREV }
			}

	every_state = {
		limit = {
		NOT = {
		is_core_of = SOV
		}
		is_core_of = SIK
		is_fully_controlled_by = SOV
		}
		RKR = {
		transfer_state = PREV
		}
	}

			if = {
				limit = {
					country_exists = SOV
		SOV = { is_subject_of = GER }
				}

				RKR = { annex_country = { target = SOV } }
			}

			if = {
				limit = {
					country_exists = RUS
		RUS = { is_subject_of = GER }
				}

				RKR = { annex_country = { target = RUS } }
			}

		}
	}

	GER_reichskommissariat_Siberia = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	has_country_flag = Formation_rk_RKR
	OR = {
		GER = { controls_state = 653 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 653
		}
	}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
	is_subject = no
	has_country_flag = Formation_rk_RKR
	NOT = { country_exists = RKR }
			country_exists = RKM
			country_exists = RKA
			country_exists = RUL
			country_exists = RTK
		}

		complete_effect = {

			GER_increase_propaganda = yes

			RKR = {
				transfer_state = 653

				GER = {
					puppet = RKR
					if = {
						limit = {
							NOT = { has_dlc = "Death or Dishonor" }
						}
						set_autonomy = { target = RKR autonomy_state = autonomy_integrated_puppet }
					}
					else = {
						set_autonomy = { target = RKR autonomy_state = autonomy_reichskommissariat }
					}
				}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RKR
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RKR }
			}
			RKR = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

			}

			every_state = {
				limit = {
					is_core_of = SOV
		NOT = { is_core_of = RKU }
		NOT = { is_core_of = RKP }
		NOT = { is_core_of = RKO }
		NOT = { is_core_of = UKR }
		NOT = { is_core_of = RKM }
		NOT = { is_core_of = RKA }
		NOT = { is_core_of = RUL }
		NOT = { is_core_of = RTK }
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
						is_ai = yes
							has_full_control_of_state = PREV
							is_in_faction_with = GER
						}
					}
					
				}
				RKR = { transfer_state = PREV }
			}

	every_state = {
		limit = {
		NOT = {
		is_core_of = SOV
		}
		is_core_of = SIK
		is_fully_controlled_by = SOV
		}
		RKR = {
		transfer_state = PREV
		}
	}

			if = {
				limit = {
					country_exists = SOV
		SOV = { is_subject_of = GER }
				}

				RKR = { annex_country = { target = SOV } }
			}

			if = {
				limit = {
					country_exists = RUS
		RUS = { is_subject_of = GER }
				}

				RKR = { annex_country = { target = RUS } }
			}

		}
	}



	GER_reichskommissariat_Czech  = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism

	OR = {
		GER = { controls_state = 9 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 9
		}
	}
	OR = {
		GER = { controls_state = 75 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 75
		}
	}

	OR = {
		GER = { controls_state = 72 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 72
		}
	}

		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
	is_subject = no
		or = {
		NOT = { is_in_faction_with = CZE }
		AND = {
		is_in_faction_with = CZE
		CZE = { is_subject = yes }
		}
		AND = {
		is_in_faction_with = CZE
		CZE = { is_subject = no }
		NOT = { CZE = { owns_state = 9 } }
		}
		}
		}

		complete_effect = {

			GER_increase_propaganda = yes

	add_state_claim = 9
	add_state_claim = 75
	add_state_claim = 72

			RKC = {
		add_state_core = 9
		add_state_core = 75
		add_state_core = 72
				transfer_state = 9
				transfer_state = 75
				transfer_state = 72
				GER = {
					puppet = RKC
					if = {
						limit = {
							NOT = { has_dlc = "Death or Dishonor" }
						}
						set_autonomy = { target = RKC autonomy_state = autonomy_integrated_puppet }
					}
					else = {
						set_autonomy = { target = RKC autonomy_state = autonomy_reichskommissariat }
					}
				}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RKC
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RKC }
			}
			RKC = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

			}

			if = {
				limit = {
					country_exists = CZE
		CZE = { is_subject_of = GER }
				}

				RKC = { annex_country = { target = CZE } }
			}

		}
	}

	GER_reichskommissariat_Slovak  = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism

	OR = {
		GER = { controls_state = 70 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 70
		}
	}
	OR = {
		GER = { controls_state = 71 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 71
		}
	}

	OR = {
		GER = { controls_state = 73 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 73
		}
	}
	OR = {
		GER = { controls_state = 664 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 664
		}
	}

	SLO = { exists = no }
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
	is_subject = no
		or = {
		NOT = { is_in_faction_with = HUN }
		AND = {
		is_in_faction_with = HUN
		HUN = { is_subject = yes }
		NOT = { HUN = { has_country_flag = GER_Accept_economic_integration } }
		}
		AND = {
		is_in_faction_with = HUN
		HUN = { is_subject = no }
		NOT = { HUN = { owns_state = 43 } }
		}
		}
		}

		complete_effect = {

			GER_increase_propaganda = yes

			SLO = {
		add_state_core = 70
		add_state_core = 71
		add_state_core = 73
		add_state_core = 664
				transfer_state = 70
				transfer_state = 71
				transfer_state = 73
				transfer_state = 664

				GER = {
					puppet = SLO
					if = {
						limit = {
							NOT = { has_dlc = "Death or Dishonor" }
						}
						set_autonomy = { target = SLO autonomy_state = autonomy_integrated_puppet }
					}
					else = {
						set_autonomy = { target = SLO autonomy_state = autonomy_reichskommissariat }
					}
				}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = SLO
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = SLO }
			}
			SLO = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

			}
		}
	}

	GER_reichskommissariat_Hungary  = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	OR = {
		GER = { controls_state = 155 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 155
		}
	}

	OR = {
		GER = { controls_state = 43 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 43
		}
	}
	OR = {
		GER = { controls_state = 154 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 154
		}
	}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
	is_subject = no
		or = {
		NOT = { is_in_faction_with = HUN }
		AND = {
		is_in_faction_with = HUN
		HUN = { is_subject = yes }
		NOT = { HUN = { has_country_flag = GER_Accept_economic_integration } }
		}
		AND = {
		is_in_faction_with = HUN
		HUN = { is_subject = no }
		NOT = { HUN = { owns_state = 43 } }
		}
		}
		}

		complete_effect = {

			GER_increase_propaganda = yes

			RKH = {
		add_state_core = 155
		add_state_core = 43
		add_state_core = 154
		add_state_core = 664
		add_state_core = 73
		add_state_core = 45
		add_state_core = 764
		add_state_core = 82
		add_state_core = 83
		add_state_core = 84
		add_state_core = 76
				transfer_state = 155
				transfer_state = 43
				transfer_state = 154
				GER = {
					puppet = RKH
					if = {
						limit = {
							NOT = { has_dlc = "Death or Dishonor" }
						}
						set_autonomy = { target = RKH autonomy_state = autonomy_integrated_puppet }
					}
					else = {
						set_autonomy = { target = RKH autonomy_state = autonomy_reichskommissariat }
					}
				}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RKH
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RKH }
			}
			RKH = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

			}

	if = {
		limit = { 
		ROM = { is_in_faction_with = GER }
		ROM = { is_subject = no }
		}
				if = {
					limit = { 
						OR = {
							GER = { controls_state = 76 }
							any_other_country = {
							NOT = { tag = ROM }
								is_in_faction_with = GER
								controls_state = 76
							}
						}
					}
					ROM = { transfer_state = 76 }
				}
				if = {
					limit = { 
						OR = {
							GER = { controls_state = 84 }
							any_other_country = {
							NOT = { tag = ROM }
								is_in_faction_with = GER
								controls_state = 84
							}
						}
					}
					ROM = { transfer_state = 84 }
				}
				if = {
					limit = { 
						OR = {
							GER = { controls_state = 83 }
							any_other_country = {
							NOT = { tag = ROM }
								is_in_faction_with = GER
								controls_state = 83
							}
						}
					}
					ROM = { transfer_state = 83 }
				}
	}

			if = {
				limit = {
					country_exists = HUN
		HUN = { is_subject_of = GER }
				}

				RKH = { annex_country = { target = HUN } }
			}

		}
	}

	GER_reichskommissariat_Expansion_RKB = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	OR = {
		OR = {
		GER = { controls_state = 155 }
		any_other_country = {
			is_ai = yes
			is_subject_of = GER
			controls_state = 155
		}
		}

		OR = {
		GER = { controls_state = 46 }
		any_other_country = {
			is_ai = yes
			is_subject_of = GER
			controls_state = 46
		}
		}
		OR = {
		GER = { controls_state = 107 }
		any_other_country = {
			is_ai = yes
			is_subject_of = GER
			controls_state = 107
		}
		}
		OR = {
		GER = { controls_state = 44 }
		any_other_country = {
			is_ai = yes
			is_subject_of = GER
			controls_state = 44
		}
		}
		OR = {
		GER = { controls_state = 48 }
		any_other_country = {
			is_ai = yes
			is_subject_of = GER
			controls_state = 48
		}
		}
	}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
	is_subject = no
	country_exists = RKH
	RKH = { is_subject_of = GER }
			has_country_flag = GER_ostreich_return	
		}

		complete_effect = {

			GER_increase_propaganda = yes

	RKH = {
				set_cosmetic_tag = RKH_RKB
		create_country_leader = {
		name = "Prince Eugen"
		desc = ""
		picture = "gfx/leaders/GER/Portrait_Prince_Eugen.dds"
		expire = "1965.1.1"
		ideology = valkist
		traits = {
			Unwelcome_commissioner
		}
		}
	}

	every_state = {
		limit = {
		or = {
			is_core_of = YUG
			is_core_of = ALB
			is_core_of = ROM
			is_core_of = BUL
			is_core_of = GRE
			is_core_of = TUR
		}
		}
		add_core_of = RKH
	}

			every_state = {
				limit = {
		or = {
			is_core_of = YUG
			is_core_of = ALB
			is_core_of = ROM
			is_core_of = BUL
			is_core_of = GRE
			is_core_of = TUR
		}
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
						is_ai = yes
							has_full_control_of_state = PREV
							is_subject_of = GER
			NOT = { has_country_flag = GER_Accept_economic_integration } 
						}
					}
					
				}
				RKH = { transfer_state = PREV }
			}

			if = {
				limit = {
					country_exists = YUG
		YUG = { is_subject_of = GER }
				}

				RKH = { annex_country = { target = YUG } }
			}

			if = {
				limit = {
					country_exists = ALB
		ALB = { is_subject_of = GER }
				}

				RKH = { annex_country = { target = ALB } }
			}

			if = {
				limit = {
					country_exists = ROM
		ROM = { is_subject_of = GER }
				}

				RKH = { annex_country = { target = ROM } }
			}

			if = {
				limit = {
					country_exists = BUL
		BUL = { is_subject_of = GER }
				}

				RKH = { annex_country = { target = BUL } }
			}

			if = {
				limit = {
					country_exists = GRE
		GRE = { is_subject_of = GER }
				}

				RKH = { annex_country = { target = GRE } }
			}

			if = {
				limit = {
					country_exists = TUR
		TUR = { is_subject_of = GER }
				}

				RKH = { annex_country = { target = TUR } }
			}

		}
	}

	GER_reichskommissariat_France = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism

	OR = {
		GER = { controls_state = 16 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 16
		}
	}

	OR = {
		GER = { controls_state = 14 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 14
		}
	}

	OR = {
		GER = { controls_state = 19 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 19
		}
	}

	OR = {
		FRA = { has_capitulated = yes }
		NOT = { country_exists = FRA }
		FRA = { is_subject = yes }
		AND = {
		country_exists = FRA 
		FRA = { is_subject = no }
		NOT = { FRA = { controls_state = 16 } }
		}
	}

		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
	is_subject = no
		or = {
		NOT = { is_in_faction_with = FRA }
		AND = {
		is_in_faction_with = FRA
		FRA = { is_subject = yes }
		}
		AND = {
		is_in_faction_with = FRA
		FRA = { is_subject = no }
		NOT = { FRA = { owns_state = 16 } }
		}
		}
		}

		complete_effect = {

			GER_increase_propaganda = yes

	add_state_claim = 1

	set_country_flag = Formation_rk_RKF

	every_state = {
		limit = {
		is_core_of = FRA
		}
		add_core_of = RKF
	}

			RKF = {
				transfer_state = 16
				GER = {
					puppet = RKF
					if = {
						limit = {
							NOT = { has_dlc = "Death or Dishonor" }
						}
						set_autonomy = { target = RKF autonomy_state = autonomy_integrated_puppet }
					}
					else = {
						set_autonomy = { target = RKF autonomy_state = autonomy_reichskommissariat }
					}
				}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RKF
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RKF }
			}
			RKF = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

		if = {
		limit = {
			FRA = {	has_completed_focus = FRA_massive_mobilization }
		}

		create_country_leader = {
			name = "Joseph Darnand"
			desc = ""
			picture = "Portrait_France_Joseph_Darnand.dds"
			expire = "1983.4.24"
			ideology = valkist
			traits = {
			Unwelcome_commissioner              
			}
		}
		}


			}

			every_state = {
				limit = {
					is_core_of = FRA
		NOT = { is_core_of = HOL }
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
			is_ai = yes
							has_full_control_of_state = PREV
							is_in_faction_with = GER
						}
					}
					
				}
				RKF = { transfer_state = PREV }
			}

			if = {
				limit = {
					country_exists = FRA
		FRA = { is_subject_of = GER }
				}

				RKF = { annex_country = { target = FRA } }
			}

		}
	}

	GER_reichskommissariat_Italy = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism

				OR = {
					GER = { controls_state = 159 }
					any_other_country = {
						is_ai = yes
						is_in_faction_with = GER
						controls_state = 159
					}
				}

				OR = {
					GER = { controls_state = 2 }
					any_other_country = {
						is_ai = yes
						is_in_faction_with = GER
						controls_state = 2
					}
				}

				OR = {
					GER = { controls_state = 115 }
					any_other_country = {
						is_ai = yes
						is_in_faction_with = GER
						controls_state = 115
					}
				}

				OR = {
		ITA = { has_capitulated = yes }
		NOT = { country_exists = ITA }
		ITA = { is_subject = yes }
		AND = {
			country_exists = ITA 
			ITA = { is_subject = no }
			NOT = { ITA = { controls_state = 2 } }
		}
				}

		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
	is_subject = no
		or = {
		NOT = { is_in_faction_with = ITA }
		AND = {
		is_in_faction_with = ITA
		ITA = { is_subject = yes }
		NOT = { ITA = { has_country_flag = GER_Accept_economic_integration } }
		}
		AND = {
		is_in_faction_with = ITA
		ITA = { is_subject = no }
		NOT = { ITA = { owns_state = 2 } }
		}
		}
		}

		complete_effect = {

			GER_increase_propaganda = yes

	add_state_claim = 114

	add_state_claim = 160


	every_state = {
		limit = {
		is_core_of = ITA
		NOT = { is_core_of = YUG }
		NOT = { is_core_of = ALB }
		NOT = { is_core_of = HUN }
		NOT = { is_core_of = ROM }
		NOT = { is_core_of = BUL }
		NOT = { is_core_of = GRE }
		NOT = { is_core_of = TUR }
		}
		add_core_of = RKI
	}

			RKI = {
				transfer_state = 2
				GER = {
					puppet = RKI
					if = {
						limit = {
							NOT = { has_dlc = "Death or Dishonor" }
						}
						set_autonomy = { target = RKI autonomy_state = autonomy_integrated_puppet }
					}
					else = {
						set_autonomy = { target = RKI autonomy_state = autonomy_reichskommissariat }
					}
				}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RKI
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RKI }
			}
			RKI = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

		if = {
		limit = {
			GER = {	has_idea = ita_exiled_tyrant }
		}

		create_country_leader = {
			name = "Nero Claudius"
			desc = "POLITICS_NERO_DESC"
			picture = "gfx//leaders//ITA//Portrait_Nero_Fascism.dds"
			expire = "1965.1.1"
			ideology = valkist
			traits = { Tyrant_of_rose }
		}
		}

			}

			every_state = {
				limit = {
					is_core_of = ITA
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
		NOT = { is_core_of = YUG }
		NOT = { is_core_of = ALB }
		NOT = { is_core_of = HUN }
		NOT = { is_core_of = ROM }
		NOT = { is_core_of = BUL }
		NOT = { is_core_of = GRE }
		NOT = { is_core_of = TUR }

					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
						is_ai = yes
							has_full_control_of_state = PREV
							is_in_faction_with = GER
						}
					}
					
				}
				RKI = { transfer_state = PREV }
			}

	if = {
		limit = { 
		HUN = { is_in_faction_with = GER }
		HUN = { is_subject = no }
		}
		HUN = { transfer_state = 103 }
	}

			if = {
				limit = {
					country_exists = ITA
		ITA = { is_subject_of = GER }
				}

				RKI = { annex_country = { target = ITA } }
			}

		}
	}

	GER_reichskommissariat_England = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism

	OR = {
		GER = { controls_state = 126 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 126
		}
	}

	OR = {
		GER = { controls_state = 122 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 122
		}
	}

	OR = {
		GER = { controls_state = 131 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 131
		}
	}

	OR = {
		ENG = { has_capitulated = yes }
		NOT = { country_exists = ENG }
		ENG = { is_subject = yes }
		AND = {
		country_exists = ENG 
		ENG = { is_subject = no }
		NOT = { ENG = { controls_state = 126 } }
		}
	}

		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
	is_subject = no
		or = {
		NOT = { is_in_faction_with = ENG }
		AND = {
		is_in_faction_with = ENG
		ENG = { is_subject = yes }
		}
		AND = {
		is_in_faction_with = ENG
		ENG = { is_subject = no }
		NOT = { ENG = { owns_state = 126 } }
		}
		}
		}

		complete_effect = {

			GER_increase_propaganda = yes

	every_state = {
		limit = {
		is_core_of = ENG
		}
		add_core_of = RKE
	}

			RKE = {
				transfer_state = 126
				GER = {
					puppet = RKE
					if = {
						limit = {
							NOT = { has_dlc = "Death or Dishonor" }
						}
						set_autonomy = { target = RKE autonomy_state = autonomy_integrated_puppet }
					}
					else = {
						set_autonomy = { target = RKE autonomy_state = autonomy_reichskommissariat }
					}
				}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RKE
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RKE }
			}
			RKE = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

			}

			every_state = {
				limit = {
					is_core_of = ENG
					NOT = { is_core_of = FRA }
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
						is_ai = yes
							OR = {
								is_subject = no
								AND = {
				is_subject = yes
				OVERLORD = { is_ai = yes  }
				}
							}
							has_full_control_of_state = PREV
							is_in_faction_with = GER
						}
					}
					
				}
				RKE = { transfer_state = PREV }
			}

			if = {
				limit = {
					country_exists = ENG
		ENG = { is_subject_of = GER }
				}

				RKE = { annex_country = { target = ENG } }
			}

		}
	}

	GER_reichskommissariat_North = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism

	OR = {
		GER = { controls_state = 138 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 138
		}
	}

	OR = {
		GER = { controls_state = 141 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 141
		}
	}

	OR = {
		GER = { controls_state = 666 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 666
		}
	}

		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_government = fascism
	is_subject = no
		or = {
		NOT = { is_in_faction_with = SWE }
		AND = {
		is_in_faction_with = SWE
		SWE = { is_subject = yes }
		}
		AND = {
		is_in_faction_with = SWE
		SWE = { is_subject = no }
		NOT = { SWE = { owns_state = 141 } }
		}
		}
		}

		complete_effect = {

			GER_increase_propaganda = yes

	add_state_claim = 124

	add_state_claim = 145

	every_state = {
		limit = {
		or = {
			is_core_of = SWE
			is_core_of = DEN
			is_core_of = NOR
			is_core_of = FIN
		}
		}
		add_core_of = RKN
	}

			RKN = {
				transfer_state = 141

				GER = {
					puppet = RKN
					if = {
						limit = {
							NOT = { has_dlc = "Death or Dishonor" }
						}
						set_autonomy = { target = RKN autonomy_state = autonomy_integrated_puppet }
					}
					else = {
						set_autonomy = { target = RKN autonomy_state = autonomy_reichskommissariat }
					}
				}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RKN
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RKN }
			}
			RKN = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

		if = {
		limit = {
			GER = {	has_country_flag = GER_Strasser_becomes_the_leader_flag }
		}

		create_country_leader = {
			name = "Sven Olov Lindholm"
			desc = ""
			picture = "gfx/leaders/GER/Portrait_Sven_Olov_Lindholm.dds"
			expire = "1965.1.1"
			ideology = nazism
			traits = {
			}
		}
		}

		if = {
		limit = {
			GER = {	has_country_flag = GER_Heydrich_becomes_the_leader_flag }
		}

		create_country_leader = {
			name = "Josef Terboven"
			desc = ""
			picture = "gfx/leaders/GER/Portrait_Josef_Terboven.dds"
			expire = "1965.1.1"
			ideology = nazism
			traits = {
			Unwelcome_commissioner
			}
		}
		}

			}

			every_state = {
				limit = {
		or = {
			is_core_of = SWE
			is_core_of = DEN
			is_core_of = NOR
		}
					NOT = { is_core_of = FIN }
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
						is_ai = yes
			OR = {
								is_subject = no
								AND = {
				is_subject = yes
				OVERLORD = { is_ai = yes  }
				}
							}
							has_full_control_of_state = PREV
							is_in_faction_with = GER
						}
					}
					
				}
				RKN = { transfer_state = PREV }
			}

			if = {
				limit = {
					country_exists = SWE
		SWE = { is_subject_of = GER }
				}

				RKN = { annex_country = { target = SWE } }
			}

		}
	}

	GER_reichskommissariat_Ibra = {

		icon = ger_reichskommissariats

		available = {
			has_government = fascism
	OR = {
		GER = { controls_state = 165 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}
		is_in_faction_with = GER
		controls_state = 165
		}
	}
	OR = {
		GER = { controls_state = 41 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}	
		is_in_faction_with = GER
		controls_state = 41
		}
	}
	OR = {
		GER = { controls_state = 169 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}	
		is_in_faction_with = GER
		controls_state = 169
		}
	}
	OR = {
		GER = { controls_state = 171 }
		any_other_country = {
		is_ai = yes
		OR = {
			is_subject = no
			AND = {
			is_subject = yes
			OVERLORD = { 
				or = { 
				is_ai = yes  
				tag = GER
				}
			}
			}
		}	
		is_in_faction_with = GER
		controls_state = 171
		}
	}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		visible = {
	has_government = fascism
	is_subject = no

	or = {
		NOT = { is_in_faction_with = SPA }
		AND = {
		is_in_faction_with = SPA
		SPA = { is_subject = yes }
		NOT = { SPA = { has_country_flag = GER_Accept_economic_integration } }
		}
		AND = {
		is_in_faction_with = SPA
		SPA = { is_subject = no }
		NOT = { SPA = { owns_state = 41 } }
		}
		NOT = { is_in_faction_with = SPR }
		AND = {
		is_in_faction_with = SPR
		SPR = { is_subject = yes }
		NOT = { SPR = { has_country_flag = GER_Accept_economic_integration } }
		}
		AND = {
		is_in_faction_with = SPR
		SPR = { is_subject = no }
		NOT = { SPR = { owns_state = 41 } }
		}
	}

		}

		complete_effect = {

			GER_increase_propaganda = yes

			RIB = {

				add_state_core = 165
				add_state_core = 794
				add_state_core = 166
				add_state_core = 172
				add_state_core = 176
				add_state_core = 792
				add_state_core = 790
				add_state_core = 791
				add_state_core = 174
				add_state_core = 171
				add_state_core = 177
				add_state_core = 167
				add_state_core = 793
				add_state_core = 41

				add_state_core = 788
				add_state_core = 168
				add_state_core = 175
				add_state_core = 170
				add_state_core = 789
				add_state_core = 173
				add_state_core = 169

				add_state_core = 290
				add_state_core = 118

				add_state_core = 178
			}

			RIB = {
				transfer_state = 41

		GER = {
		puppet = RIB
		if = {
			limit = {
			NOT = { has_dlc = "Death or Dishonor" }
			}
			set_autonomy = { target = RIB autonomy_state = autonomy_integrated_puppet }
		}
		else = {
			set_autonomy = { target = RIB autonomy_state = autonomy_reichskommissariat }
		}
		}

		random_country = {
		limit = {
			is_faction_leader = yes
			is_in_faction_with = RIB
		}
		random_enemy_country = {
			limit = {
			NOT = { has_war_with = RIB }
			}
			RIB = {
			add_to_war = {
				targeted_alliance = PREV.PREV
				enemy = PREV
				hostility_reason = asked_to_join
			}
			}
		}
		}

			}

			every_state = {
				limit = {
		is_core_of = RIB
		NOT = { is_claimed_by = GER }
		NOT = { is_core_of = GER }
					OR = {
						is_fully_controlled_by = GER
						any_other_country = {
						is_ai = yes
							has_full_control_of_state = PREV
							is_in_faction_with = GER
						}
					}
					
				}
				RIB = { transfer_state = PREV }
			}

	if = {
		limit = { 
		NOT = { POR = { is_in_faction_with = GER } }
		}
		every_state = {
		limit = {
			is_core_of = POR
		}
		add_core_of = RIB
		}
	}


		}
	}

}

