is_owned_or_controlled_by_ROOT_or_subject = {
	OR = {
		controller = {
			OR = {
				tag = ROOT
				is_subject_of = ROOT
			}
		}
		owner = {
			OR = {
				tag = ROOT
				is_subject_of = ROOT
			}
		}
	}
}

is_controlled_by_ROOT_or_ally = {
	controller = {
		is_ally_with = ROOT
	}
}
